
keyspace: keyspace1
table: standard1

insert:
  partitions: fixed(1)

queries:
  range_read:
    cql: select * from standard1 where token(key) >= -4570794944214759424 and token(key) < 4621334552741592064 limit 1
    fields: samerow

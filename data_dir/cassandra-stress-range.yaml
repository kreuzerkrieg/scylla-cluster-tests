keyspace: keyspace1
table: standard1

insert:
  partitions: fixed(1)

queries:
  range_read:
    cql: select * from standard1 where token(key) > 1000000 and token(key) < 100000000 LIMIT 1000 ALLOW FILTERING BYPASS CACHE
    fields: samerow
